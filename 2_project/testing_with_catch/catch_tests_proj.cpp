#include "catch.h"

#include <algorithm>

// the TEST_CASE macro defines a test case, called "computation of pi" 
// as a label for the test case we use [correctness] 
// TEST_CASE("generate an enhance picture", "[correctness]") {

//   // expression in REQUIRE must evaluate to true or the test fails 
//   REQUIRE(*max_element(gray_pic.begin(), gray_pic.end()) > 0) ;
// }

